<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Me desculpa, meu gatinho</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;600;800&family=Pacifico&display=swap" rel="stylesheet">
  <style>
    :root{
      --pink-1: #ffe6f0;
      --pink-2: #ffb6d5;
      --pink-3: #ff84bf;
      --accent: #ffffff;
      --glass: rgba(255,255,255,0.25);
      --shadow: rgba(0,0,0,0.12);
    }

    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; font-family: Montserrat, system-ui, Arial, sans-serif; }

    body {
      background: linear-gradient(160deg, var(--pink-1), var(--pink-2) 40%, var(--pink-3) 100%);
      display: flex; align-items: center; justify-content: center; padding: 32px;
    }

    .card {
      width: min(980px, 96%);
      min-height: 520px;
      border-radius: 18px;
      background: linear-gradient(180deg, rgba(255,255,255,0.65), rgba(255,255,255,0.45));
      box-shadow: 0 10px 30px var(--shadow);
      display: grid; grid-template-columns: 1fr 360px; overflow: hidden; position: relative;
      backdrop-filter: blur(6px);
    }

    .left {
      padding: 48px 56px;
      display: flex; flex-direction: column; align-items: flex-start; justify-content: center; gap: 18px; position: relative;
    }

    .big-text {
      font-family: 'Pacifico', cursive;
      font-size: 3rem;
      color: #ff3366;
      line-height: 1.2;
      animation: pulse 2s infinite alternate;
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      100% { transform: scale(1.05); }
    }

    /* Exemplo de botão */
    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 12px;
      background-color: #ff84bf;
      color: white;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
    }

    .btn:hover {
      background-color: #ffb6d5;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="left">
      <div class="big-text">Me desculpa, meu gatinho 😿</div>
      <p>Eu sei que errei, mas prometo que vou me esforçar para fazer tudo certo de agora em diante. 💖</p>
      <button class="btn">Aceitar pedido de desculpas</button>
    </div>
    <!-- Aqui você pode adicionar uma coluna direita com imagens ou qualquer decoração -->
  </div>
</body>
</html>

